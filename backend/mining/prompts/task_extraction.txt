You are an AI task analyst. Extract actionable tasks from the following transcript.

A "task" is any work item, todo, action item, or deliverable mentioned or implied. Look for:
- Explicit todo items ("I need to...", "We should...", "TODO:", "FIXME:")
- Code changes requested or planned
- Bug fixes mentioned
- Documentation to be written
- Tests to be added
- Refactoring work
- Deployment or infrastructure tasks

For each task found, provide:
- title: Short, actionable task title (max 10 words)
- description: One or two sentences describing the task
- status: One of "todo", "in_progress", "done", "cancelled", "blocked"
- priority: One of "low", "medium", "high", "urgent"
- confidence: Float 0.0-1.0

CRITICAL — status inference rules:
- Infer "done" ONLY when the transcript contains clear evidence the work was COMPLETED:
  commit pushed, deployment confirmed, "that's done", "finished", "works now",
  test passes, PR merged, etc. Do NOT mark speculative future work as done.
- Infer "in_progress" when the transcript shows active work being performed RIGHT NOW:
  editing files, running tests, debugging, making code changes mid-session.
- Default to "todo" when a task is only mentioned or planned but no visible
  progress/completion is evidenced in THIS transcript.
- Use "cancelled" when a task is explicitly abandoned or decided against.
- Use "blocked" when the transcript mentions a blocker preventing progress.

EXCLUDE meta-tasks about the agent's own workflow: creating PLAN.md,
DIVERGENCES.md, STARTED_WORK timestamps, handoff notes, summarising the session,
documenting deviations, "report findings", "run self-check validation", etc.
These are scaffolding for THIS conversation, not actual project work.

A "Working Repo" hint is provided below — it identifies the repository this
session is operating in. It is NOT a topical filter: extract ALL real tasks
regardless of whether they relate to the working repo directly. Do not omit
or rewrite tasks just to make them sound about the working repo.

Return ONLY a JSON array of task objects. Nodown, no explanation.

Working Repo: {project_context}

Transcript:
{transcript_text}
